{
  "name": "template-02",
  "version": "1.0.0",
  "private": true,
  "description": "DragonWorkflows Template 02 — Bold Dark. Reference implementation: electrician. Built with Eleventy.",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "preview": "cross-env PATH_PREFIX=/ eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cross-env": "^10.1.0"
  }
}
